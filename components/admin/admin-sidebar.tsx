/**
 * Superseded by ConsoleShell, which renders the sidebar, mobile drawer and tab
 * bar as one unit. Kept as a no-op so a stale import can't render a second nav.
 */
export function AdminSidebar() {
  return null
}
