"use client"

/**
 * Superseded by TabBar. Kept as a thin alias so any straggling import keeps
 * rendering the current chrome instead of a second, divergent tab bar.
 */
export { TabBar as BottomNav } from "@/components/layout/tab-bar"
