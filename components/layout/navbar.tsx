"use client"

/**
 * Superseded by AppBar. Kept as a thin alias so any straggling import keeps
 * rendering the current chrome instead of a second, divergent header.
 */
export { AppBar as Navbar } from "@/components/layout/app-bar"
